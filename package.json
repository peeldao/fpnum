{
  "name": "fpnum",
  "version": "0.0.14",
  "type": "module",
  "description": "A tiny lib for fixed-point numbers",
  "main": "dist/cjs/fixedpoint.js",
  "module": "dist/esm/fixedpoint.js",
  "types": "dist/cjs/fixedpoint.d.ts",
  "scripts": {
    "test": "vitest",
    "build": "yarn build:esm && yarn build:cjs",
    "build:esm": "tsc --project ./tsconfig.esm.json",
    "build:cjs": "tsc --project ./tsconfig.cjs.json"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2",
    "vitest": "^0.34.5"
  },
  "files": [
    "dist"
  ]
}
